[build]
# Root-level Dockerfile — build context is the repo root (Railway default).
# backend/Dockerfile is kept for Docker Compose (context = ./backend).
dockerfilePath = "Dockerfile"

[deploy]
healthcheckPath    = "/api/health"
healthcheckTimeout = 30
restartPolicyType  = "on_failure"
